UserParameter=jolokia.read[*],<%= $read_script %> "<%= $cache_dir %>" "$1" "$2" "$3" "$5" "$6" "$7" "$8" "$9"
UserParameter=jolokia.discovery[*],<%= $discovery_script %> "$1" "$2" "$3" "$5" "$6" "$7" "$8" "$9"
